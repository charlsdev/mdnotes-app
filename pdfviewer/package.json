{
  "name": "mdnotes-pdfviewer",
  "private": true,
  "type": "module",
  "description": "Visor de PDF (pdf.js) bundleado a un HTML autónomo, embebido en el WebView del app. NO forma parte del bundle de la app; ver build.mjs.",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "pdfjs-dist": "^4.10.38"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
